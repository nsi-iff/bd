%h2 Editar Livro

= render 'form'
- if @livro.editavel?
  = link_to 'Submeter', livro_submeter_path(@livro)
- if @livro.pendente? and current_usuario.gestor?
  = link_to 'Aprovar', livro_aprovar_path(@livro)
