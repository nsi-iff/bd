%h1 Acessar
%p Preencha os campos abaixo para acessar a Biblioteca Digital da EPCT como usuário cadastrado.
%br

= semantic_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f|
  = f.input :email, label: 'E-mail', required: false
  = f.input :password, label: 'Senha', required: false
  = f.submit "Entrar", style: 'float: none'

= render "links"
