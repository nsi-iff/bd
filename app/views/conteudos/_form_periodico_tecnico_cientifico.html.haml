= semantic_form_for @conteudo, url: url_para_form_conteudo(@conteudo) do |f|
  %nav{ id: 'menu_abas'}
    %ul
      %li{ class: 'sel_aba', data: { padrao: '' }}
        %a{:href=>"#metadados_basicos"} Dados Básicos
      %li{ class: 'sel_aba'}
        %a{:href=>"#metadados_complementares"} Dados Complementares

  #metadados_basicos.aba
    = render 'conteudos/dados_basicos', :f => f

  #metadados_complementares.aba
    = f.input :editora, hint: 'Organização que coordena a publicação do Periódico'
    = f.input :local_publicacao, label: 'Local da publicação'
    = f.input :ano_primeiro_volume, label: 'Ano do primeiro volume'
    = f.input :ano_ultimo_volume, label: 'Ano do último volume'
    = f.input :resumo
    = f.input :direitos
    %span.clear

  = f.submit 'Salvar'
