%h3 Cesta de Grãos
- if session[:cesta].blank?
  %p Não há nenhum grão na cesta.
- else
  #items
    - session[:cesta].map {|grao_id| Grao.find(grao_id) }.each do |grao|
      .item
        = link_to grao.key, conteudo_path(grao.conteudo)
        = grao.tipo_humanizado
        = "#{grao.created_at.strftime('%d/%m/%Y')}"

//  = link_to "Ver todos", escrivaninha_usuario_path(current_usuario)
