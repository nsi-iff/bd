- if current_usuario.present? && can?(:ter_estante, current_usuario)
  %h3 Estante
  - current_usuario.estante.each do |conteudo|
    .item
      = link_to conteudo.titulo, conteudo
      %span.info
        = "#{conteudo.nome_contribuidor} - #{conteudo.created_at.strftime('%d/%m/%Y')}"
      - if current_usuario.favoritos.include? conteudo
        = link_to 'Remover favorito', send("#{conteudo.class.name.underscore}_remover_favorito_path", conteudo)
      - else
        = link_to 'Favoritar', send("#{conteudo.class.name.underscore}_favoritar_path", conteudo)
  - if current_usuario.estante.blank?
    %p Não há nenhum conteúdo pendente.
