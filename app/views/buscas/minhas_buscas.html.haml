.minhas_buscas.index
  %h1 Minhas Buscas

  - @buscas.each do |busca|
    .item
      = link_to busca.titulo, busca_path(busca)
      - if busca.descricao.present?
        %p #{busca.descricao}
      .links
        = link_to 'Editar', edit_busca_path(busca)
        = link_to 'Deletar', busca_path(busca), method: "delete", confirm: 'Tem certeza?'
        - if busca.mala_direta?
          = link_to 'remover da mala direta', busca_remover_mala_direta_path(busca)
        - else
          = link_to 'cadastrar mala direta', busca_cadastrar_mala_direta_path(busca)